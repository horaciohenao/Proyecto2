lista = [] # Crear una lista vacía

# Agregar elementos a la lista
lista.push(10)
lista.push(20)
lista.push(30)

# Acceder a elementos individuales
puts "Elemento en la posición 1: #{lista[0]}"
puts "Elemento en la posición 2: #{lista[1]}"
puts "Elemento en la posición 3: #{lista[2]}"
